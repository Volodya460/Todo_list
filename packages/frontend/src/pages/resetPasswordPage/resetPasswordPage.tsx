import * as React from 'react';
import { ResetPasswordForm } from '~shared/components/forms/resetPasswordForm/resetPasswordForm';

export default function ResetPasswordPage(): React.ReactNode {
	return (
		<section>
			<ResetPasswordForm />
		</section>
	);
}
